name: Deploy FastAPI

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy app
        run: |
          rsync -a --delete ${{ github.workspace }}/ /home/ubuntu/ctspk/fastapi/
